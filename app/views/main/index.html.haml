
- content_for :main_content do
  #application


- content_for :js_links do
  :javascript
    $(function() {
      var app = AdminJS.app({
        el: "#application",
        locale: 'es',
        callbacks: AdminJS.test.Callbacks()
      });
      
      app.initialize();
     });
